import { FirebasemanualesModel } from './firebasemanuales.model.model';

describe('FirebasemanualesModel', () => {
  it('should create an instance', () => {
    expect(new FirebasemanualesModel()).toBeTruthy();
  });
});
